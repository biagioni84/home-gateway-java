# Tell Lombok to copy @Lazy from fields to generated constructor parameters.
# Without this, @Lazy is silently ignored on constructor-injected fields,
# which leaves circular Spring dependencies unbroken.
lombok.copyableAnnotations += org.springframework.context.annotation.Lazy
